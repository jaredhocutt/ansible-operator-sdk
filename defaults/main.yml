---

# Usually I would default this to latest, but since this is pre-release, latest is not yet supported
operator_sdk_version: v0.6.0

operator_sdk_install_dir: /usr/local/bin
operator_sdk_binary_path: "{{ operator_sdk_install_dir }}/operator-sdk"

operator_sdk_download_url: https://github.com/operator-framework/operator-sdk/releases/download/{{ operator_sdk_version }}/operator-sdk-{{ operator_sdk_version }}-x86_64-linux-gnu
